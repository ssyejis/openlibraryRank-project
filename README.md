# openlib-rank

Local project to search and list GitHub repositories by stars with tag-based filtering.

## 개발 실행

Run backend:

```
cd backend
npm install
npm run dev
```

Run frontend:

```
cd frontend
npm install
npm start
```